name: app_infiltrazioni
description: App per sopralluoghi infiltrazioni con autosalvataggio
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  image_picker: ^1.1.2
  path_provider: ^2.1.3
  path: ^1.9.0
  speech_to_text: ^6.5.1   # NUOVO: Riconoscimento vocale
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
